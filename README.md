# myvkhl

This repository contains a simple Vulkan + GLFW + ImGui sample based on `vulkan.hpp`.

## Building

Use CMake to build. Make sure you have the Vulkan SDK, GLFW, and ImGui available on your system.

```
mkdir build
cd build
cmake ..
make
```

Run the resulting `myvkhl` executable.

